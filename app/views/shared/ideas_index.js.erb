$('div.grid_items').append($("<%= escape_javascript(render @view_type)%>").hide()).find('[class*="idea_item"]').slideDown('slow');
$('#load_more_link').replaceWith("<%= escape_javascript(link_to_next_page(@ideas, t('helpers.links.more'), :remote => true, :id=>'load_more_link', :params => {:view => params[:view]}))%>");
