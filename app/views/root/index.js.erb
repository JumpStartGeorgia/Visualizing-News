console.log("running...");
$('#visual_list').append($("<%= escape_javascript(render @view_type)%>").hide()).find('div.grid_items:last').slideDown('slow');
$('#load_more_link').replaceWith("<%= escape_javascript(link_to_next_page(@stories, t('helpers.links.more'), :remote => true, :id=>'load_more_link', :params => {:view => params[:view]}))%>");
