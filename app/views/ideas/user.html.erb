<div id="post_idea">

  <%= render "shared/ideas_form" %>

  <% title_image @user.avatar if @user.avatar %>
  <%= page_title t('.title', :name => @user.nickname) %>

  <!--
  <% if !@user.organization_users.empty? %>
	  <h2><%= OrganizationUser.human_attribute_name(:organization) %></h2>
	  <div style="display: inline-block;">
		  <% if @user.organization_users.first.organization.logo_file_name %>
			  <%= link_to(image_tag(@user.organization_users.first.organization.logo.url),
					  organization_path(@user.organization_users.first.organization_id)) %>
		  <% else %>
			  <%= link_to @user.organization_users.first.organization.name,
						  organization_path(@user.organization_users.first.organization_id) %>
		  <% end %>
	  </div>
	  <div style="display: inline-block;">
		  <%= link_to t('helpers.links.website'), @user.organization_users.first.organization.url, :class => 'btn btn-mini' if @user.organization_users.first.organization.url %>
	  </div>
  <% end %>
  -->

  <%= render "shared/ideas_filter_bar" %>

  <div id="idea_list" class="viz">
    <%= render @view_type %>
  </div>

  <a id="load_more_link" href="#" style="display: none;"><%= t('helpers.links.more') %></a>


</div>
