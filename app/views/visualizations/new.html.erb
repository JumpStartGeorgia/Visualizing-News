<%- model_class = Visualization -%>
<%= page_title t '.title', :default => t('helpers.titles.new', :model => model_class.model_name.human,
                                 :default => "New #{model_class.model_name.human}") %>

<div class="form-actions">
	<%= link_to t('helpers.links.list'), organization_path(params[:organization_id]),
  :class => 'btn' %>
</div>

<%= render :partial => 'form' %>
