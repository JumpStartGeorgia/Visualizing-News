<div class="grid_items">
  <div class="grid_story_row clear">
	<% @stories.each_with_index do |story, index| %>

		  <div class="grid_story_item">
		    <div class="visual">
		      <%= link_to(image_tag(story.visual.url(:thumb)), visual_path(story.id)) if story.visual_file_name %>
		    </div>
		    <div class="title">
		      <h2><%= link_to story.title, visual_path(story.id) %></h2>
					<div class="item_footer clear">
						<div class="comment_count">Comments: 123</div>
						<div class="like_count">Likes: -234</div>
					</div>
		    </div>
		  </div>

		<% if (index + 1) % 4 == 0 %>
		  </div>
      <div class="grid_story_row clear">
		<% end %>
	<% end %>
	</div>

</div>
