<%= simple_format @message.message %>

<%= simple_format @message.org_message %>

<%= link_to visualization_url(:id => @message.url_id, :locale => @message.locale),
						visualization_url(:id => @message.url_id, :locale => @message.locale) %>
